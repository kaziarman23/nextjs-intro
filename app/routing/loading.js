import React from "react";

function loading() {
    return (
        <div className="flex justify-center items-center">
            {" "}
            <h1>Routing page is loading...</h1>
        </div>
    );
}

export default loading;
